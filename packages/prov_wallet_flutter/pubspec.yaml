name: prov_wallet_flutter
description: A new flutter plugin project.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  dart_code_metrics: ^4.9.0
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.figure.prov_wallet_flutter
        pluginClass: ProvWalletFlutterPlugin
      ios:
        pluginClass: ProvWalletFlutterPlugin
      macos:
        pluginClass: ProvWalletFlutterPlugin
