///
/// Must match values in Kotlin and Swift.
///
enum CipherServiceErrorCode {
  accessError,
  accountKeyNotFound,
  addSecItem,
  dataPersistence,
  invalidArgument,
  publicKeyError,
  secKeyNotFound,
  unknown,
  upgradeError,
  unsupportedAlgorithm,
}
